# 后端工程启动流程

> 🎯 本阶段目标：
> **第一个「能跑起来、能提交、像工程」的版本（v0.1.0）**

------

# 🚀 从 0 到 v0.1.0：工程级启动流程

## 当前状态确认

- ✅ 已有 Git 仓库
- ✅ 已有目录结构
- ✅ 已有 README / docs / .gitignore
- ❌ 还没有任何可运行代码

------

# Step 1：生成后端基础工程（先后端）

> 原因：**前端依赖接口，接口必须先“存在”**

------

## 1️⃣ 用 Spring Initializr（标准做法）

### 选择配置

- Project：**Maven**
- Language：**Java**
- Spring Boot：**3.x（稳定版）**
- Group：`com.wsw`
- Artifact：`fitness-system`
- Name：`fitness-system`
- Java：**17**

![001](images\001.png)

### 依赖（只选这些）

- ✅ Spring Web
- ✅ Spring Security（先加，后面不痛）
- ✅ Validation
- ✅ Lombok
- ⛔ 先不要 MyBatis / JPA（避免一上来复杂）

📌 **原则**：

> 先“跑起来”，再“复杂起来”

------

![002](images\002.png)

## 2️⃣ 创建后工程结构

```
fitness-system/
├── src/
└── pom.xml
```

- pom文件内容

  ```xml
  <?xml version="1.0" encoding="UTF-8"?>
  <project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
      <modelVersion>4.0.0</modelVersion>
      <parent>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-starter-parent</artifactId>
          <version>3.5.9</version>
          <relativePath/> <!-- lookup parent from repository -->
      </parent>
      <groupId>com.wsw</groupId>
      <artifactId>fitness-system</artifactId>
      <version>0.0.1-SNAPSHOT</version>
      <properties>
          <java.version>17</java.version>
      </properties>
      <dependencies>
          <dependency>
              <groupId>org.springframework.boot</groupId>
              <artifactId>spring-boot-starter-security</artifactId>
          </dependency>
          <dependency>
              <groupId>org.springframework.boot</groupId>
              <artifactId>spring-boot-starter-validation</artifactId>
          </dependency>
          <dependency>
              <groupId>org.springframework.boot</groupId>
              <artifactId>spring-boot-starter-web</artifactId>
          </dependency>
  
          <dependency>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <optional>true</optional>
          </dependency>
          <dependency>
              <groupId>org.springframework.boot</groupId>
              <artifactId>spring-boot-starter-test</artifactId>
              <scope>test</scope>
          </dependency>
          <dependency>
              <groupId>org.springframework.security</groupId>
              <artifactId>spring-security-test</artifactId>
              <scope>test</scope>
          </dependency>
      </dependencies>
  
      <build>
          <plugins>
              <plugin>
                  <groupId>org.apache.maven.plugins</groupId>
                  <artifactId>maven-compiler-plugin</artifactId>
                  <configuration>
                      <annotationProcessorPaths>
                          <path>
                              <groupId>org.projectlombok</groupId>
                              <artifactId>lombok</artifactId>
                          </path>
                      </annotationProcessorPaths>
                  </configuration>
              </plugin>
              <plugin>
                  <groupId>org.springframework.boot</groupId>
                  <artifactId>spring-boot-maven-plugin</artifactId>
                  <configuration>
                      <excludes>
                          <exclude>
                              <groupId>org.projectlombok</groupId>
                              <artifactId>lombok</artifactId>
                          </exclude>
                      </excludes>
                  </configuration>
              </plugin>
          </plugins>
      </build>
  
  </project>
  
  ```

- 

------

## 3️⃣ 第一次后端 commit（里程碑）

### 先确认能启动：

> Idea打开项目，启动

浏览器访问：

```
http://localhost:8080
```

成功如图：

![003](images\003.png)

------

### 提交 Git（非常标准）

```bash
git add backend
git commit -m "chore(fitness-backend): 初始化Spring Boot基础工程"
```

📌 到这里你已经是 **“工程启动成功”**

------

# Step 2：加一个“存活接口”（证明系统活着）

> 业务项目一定要有 **health check**

------

## 1️⃣ 新建一个 controller

```java
@RestController
@RequestMapping("/api/health")
public class HealthController {

    @GetMapping
    public String health() {
        return "OK";
    }
}
```

访问：

```
GET /api/health
```

返回 `OK`

------

## 2️⃣ 提交 commit

```bash
git commit -m "feat(system): 新增健康检查接口"
```

📌 这一步非常重要：
以后部署 / 运维 / 监控都会用这个接口。

------

# Step 3：先“关掉安全”，但方式要专业

你已经加了 Spring Security，
如果不处理，现在接口是 401。

------

## 1️⃣ 临时放行所有请求（开发期）

```java
@Bean
public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
    http
        // 1. 前后端分离：关闭 CSRF（因为使用JWT，无状态）
        .csrf(csrf -> csrf.disable())
        // 2. 接口权限规则
        .authorizeHttpRequests(auth -> auth
            // 所有接口都放行（不需要鉴权）
            .anyRequest().permitAll()
        );
    return http.build();
}
```

📌 这是**临时策略**，但写法是正式的。

------

## 2️⃣ 提交 commit（语义一定要对）

```bash
git commit -m "chore(auth): 开发阶段暂时放行全部接口"
```

------

# Step 4：引入数据库

> 业务系统 **数据库一定要早接**

------

## 1️⃣ 引入依赖（MyBatisPlus + MySQL）

```xml
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
    <version>3.5.5</version>
</dependency>
<dependency>
    <groupId>com.mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>com.alibaba</groupId>
    <artifactId>druid-spring-boot-starter</artifactId>
    <version>1.2.27</version>
    <scope>compile</scope>
</dependency>
```

------

## 2️⃣ application.yml（先能连上）

```yml
spring:
    datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.127.150:3306/student_fitness_info_db?rewriteBatchedStatements=true
    username: root
    password: "xxx"
```

------

## 3️⃣ 提交 commit

```bash
git commit -m "chore(db): 引入MyBatis-Plus并配置数据源"
```

------

# 完成

- ✅ 能启动的后端服务
- ✅ 标准工程结构
- ✅ 健康检查接口
- ✅ 数据库连接能力
- ✅ 干净、可回溯的 Git 记录

👉 **这是一个真实项目的 v0.1.0**

------

