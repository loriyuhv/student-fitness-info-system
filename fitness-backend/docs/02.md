

# 一、现在的状态

- ✔ RBAC 表结构：**设计合理、字段语义清晰、考虑了审计 & 风控**
- ✔ DDD 分层：**层次清楚，没有 Controller 直接怼 Mapper**
- ✔ 权限粒度：**到 perm_code，能直接对接 Spring Security**

👉 **差的只剩一件事**：
**把「表 → 代码 → 认证流程 → 安全框架」串成一条线**

我们就做这一件事。

------

# 二、整体目标：认证模块要干什么？

你这个系统的 **“用户认证模块”** 最终要完成：

```
账号 + 密码
   ↓
Spring Security 认证
   ↓
加载用户角色 + 权限
   ↓
签发 JWT
   ↓
前端携带 JWT 访问系统
```

并且满足：

- 使用你设计的 `sys_user / sys_role / sys_permission`
- 符合 **DDD 分层**
- **Git commit 有节奏、有语义**

------

# 三、第一步：定义「认证子域」的边界（DDD 思维）

先别写代码，**先想边界**。

## 1️⃣ 认证模块 = application.auth

它只关心三件事：

| 事情          | 来自哪里                            |
| ------------- | ----------------------------------- |
| 校验账号密码  | sys_user                            |
| 获取角色/权限 | sys_user_role / sys_role_permission |
| 生成 token    | infrastructure.security             |

👉 **它不关心**：

- HTTP（那是 interfaces）
- JWT 具体怎么生成（那是 infrastructure）

------

## 2️⃣ 认证模块的核心对象

你已经规划好了，我帮你“定型”一下：

```text
application.auth/
├── AuthApplicationService	# 用例服务（登录）
└── dto
	├── LoginCommand       	# 登录请求（DTO）
	└── LoginResult        	# 登录返回结果
```

**非常标准，别动。**

------

# 四、第二步：从数据库 → 领域模型（不要急着写 Mapper）

## 1️⃣ User 领域模型（domain 层）

> domain ≠ entity
> domain 是「业务语义」

```java
package com.wsw.fitnesssystem.domain.user;

@Data
@AllArgsConstructor
public class User {
    private Long userId;
    private String username;
    private String password;
    private Integer status;

    public boolean isEnabled() {
        return status != null && status == 1;
    }
}
```

❗注意

- **没有 MyBatis 注解**
- **没有表字段一一对应**
- 只保留 **认证需要的字段**

------

## 2️⃣ 为什么这样做？

因为你后面会发现：

> **Security 关心的是：能不能登录、有什么权限**
> 不是手机号、备注、来源

这就是 DDD 的**降噪能力**。

------

### ✅ 第一次 Git commit（只做结构，不写逻辑）

```bash
git add .
git commit -m "chore: initialize auth domain model and package structure"
```

**你现在在干什么？**
👉 在给系统“搭骨架”，不是写功能。

------

# 五、第三步：基础设施层（Security 的落脚点）

## 1️⃣ UserRepository

在 **DDD 分层**里：

> **`UserRepository` 是「领域层（domain）」的接口**
>  **实现类在 infrastructure.persistence 里**

也就是：

```
domain
 └── user
      └── repository
           └── UserRepository   ← 接口（领域层）

infrastructure
 └── persistence
      └── repository
           └── UserRepositoryImpl  ← 实现（MyBatis / JPA）
```

👉 **security 层只能依赖 domain，不允许直接依赖 MyBatis / Mapper**

### UserRepository接口

📄 接口定义（只描述能力，不写 SQL）

```java
package com.wsw.fitnesssystem.domain.user;

import java.util.Optional;
import java.util.Set;

public interface UserRepository {
    /**
     * 根据用户名查询用户
     */
    Optional<User> findByUsername(String username);

    /**
     * 查询用户权限标识集合
     */
    Set<String> findPermissions(Long userId);
}

```

⚠️ **注意关键点**

- 返回的是 **领域实体 `User`**
- 方法名是**业务语义**，不是技术语义
- **没有 MyBatis / JPA / SQL**

### UserRepository 实现类

```java
package com.wsw.fitnesssystem.infrastructure.persistence.repository;

import com.wsw.fitnesssystem.domain.user.User;
import com.wsw.fitnesssystem.domain.user.UserRepository;
import com.wsw.fitnesssystem.auth.infrastructure.persistence.entity.SysUser;
import com.wsw.fitnesssystem.auth.infrastructure.persistence.mapper.SysUserMapper;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Repository;

import java.util.Optional;
import java.util.Set;

@Repository
@RequiredArgsConstructor
public class UserRepositoryImpl implements UserRepository {
    private final SysUserMapper sysUserMapper;

    @Override
    public Optional<User> findByUsername(String username) {
        SysUser sysUser = sysUserMapper.selectByUsername(username);
        if (sysUser == null) {
            return Optional.empty();
        }
        return Optional.of(toDomain(sysUser));
    }

    @Override
    public Set<String> findPermissions(Long userId) {
        return sysUserMapper.selectPermissionsByUserId(userId);
    }

    private User toDomain(SysUser sysUser) {
        return new User(
            sysUser.getUserId(),
            sysUser.getUsername(),
            sysUser.getPassword(),
            sysUser.getStatus()
        );
    }
}

```

### DDD分层架构对象说明

DDD 下你应该有两套对象 👇

| 类型      | 位置                       | 用途       |
| --------- | -------------------------- | ---------- |
| `User`    | domain                     | 领域实体   |
| `SysUser` | infrastructure.persistence | 持久化对象 |

### SysUser

```java
package com.wsw.fitnesssystem.infrastructure.persistence.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;

import java.io.Serial;
import java.io.Serializable;
import java.time.LocalDateTime;

/**
 * 系统用户实体
 * 对应表：sys_user
 * 注意：
 * - MyBatis-Plus 持久化对象/实体（PO / Entity）
 * - 不直接返回给前端
 * - 不承载业务逻辑
 *
 * @author loriyuhv
 * @version 1.0 2026/1/11 8:28
 * @since 1.0
 */
@Data
@TableName("sys_user") // 启用 MP 代理
public class SysUser implements Serializable {
    @Serial
    private static final long serialVersionUID = 1L;

    /** 用户ID */
    @TableId(type = IdType.AUTO)
    private Long userId;

    /** 登录账号（学号 / 工号） */
    private String username;

    /** 登录密码（BCrypt 等加密） */
    private String password;

    /** 昵称 */
    private String nickName;

    /** 手机号 */
    private String phoneNumber;

    /** 邮箱 */
    private String email;

    /** 备注 */
    private String remark;

    /** 来源 */
    private String source;

    /** 状态：0-禁用，1-启用 */
    private Integer status;

    /** 逻辑删除：0-未删除，1-已删除 */
    @TableLogic(value = "0", delval = "1")
    private Integer deleted;

    /** 创建时间：只在插入时自动填充 */
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;

    /** 更新时间：插入和更新时都填充 */
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;
}

```

### SysUserMapper

```java
package com.wsw.fitnesssystem.infrastructure.persistence.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.wsw.fitnesssystem.auth.infrastructure.persistence.entity.SysUser;
import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;
import org.apache.ibatis.annotations.Select;

import java.util.Set;

@Mapper
public interface SysUserMapper extends BaseMapper<SysUser> {
    @Select("SELECT * FROM sys_user WHERE username = #{username} AND deleted = 0;")
    com.wsw.fitnesssystem.auth.infrastructure.persistence.entity.SysUser selectByUsername(@Param("username") String username);

    @Select("SELECT DISTINCT p.perm_code " +
        "FROM sys_user_role ur " +
        "JOIN sys_role r" +
        "                      ON ur.role_id = r.role_id" +
        "                          AND r.deleted = 0" +
        "                          AND r.status = 1" +
        "                 JOIN sys_role_permission rp" +
        "                      ON r.role_id = rp.role_id" +
        "                          AND rp.deleted = 0" +
        "                          AND rp.status = 1" +
        "                 JOIN sys_permission p" +
        "                      ON rp.perm_id = p.perm_id" +
        "                          AND p.deleted = 0" +
        "                          AND p.status = 1" +
        "        WHERE ur.user_id = #{userId}" +
        "          AND ur.deleted = 0" +
        "          AND ur.status = 1")
    Set<String> selectPermissionsByUserId(@Param("userId") Long userId);
}

```



## 2️⃣ UserDetails 适配器（关键一步）

Spring Security **不认识你的 User**，
它只认识 `UserDetails`。

```java
package com.wsw.fitnesssystem.infrastructure.security.model;

import com.wsw.fitnesssystem.domain.user.User;
import lombok.Getter;
import org.springframework.security.core.GrantedAuthority;
import org.springframework.security.core.authority.SimpleGrantedAuthority;
import org.springframework.security.core.userdetails.UserDetails;

import java.util.Collection;
import java.util.Set;

@Getter
public class LoginUser implements UserDetails {
    private final User user;

    /**
     * 当前用户的权限集合
     */
    private final Set<String> authorities;

    public LoginUser(User user, Set<String> authorities) {
        this.user = user;
        this.authorities = authorities;
    }

    @Override
    public Collection<? extends GrantedAuthority> getAuthorities() {
        return authorities.stream()
            .map(SimpleGrantedAuthority::new)
            .toList();
    }

    /* ================== UserDetails 必须实现的方法 ================== */
    @Override
    public String getPassword() {
        return user.getPassword();
    }

    @Override
    public String getUsername() {
        return user.getUsername();
    }

    @Override
    public boolean isEnabled() {
        return user.isEnabled();
    }

    @Override
    public String toString() {
        return "LoginUser{" +
            "userId=" + user.getUserId() +
            ", username='" + user.getUsername() + '\'' +
            ", authorities=" + authorities +
            '}';
    }
}

```

👉 **这一层是“反腐层”**
把你优雅的领域模型，翻译成 Security 能懂的语言。

------

## 2️⃣ 自定义 UserDetailsService

```java
package com.wsw.fitnesssystem.infrastructure.security.service;

import com.wsw.fitnesssystem.domain.user.User;
import com.wsw.fitnesssystem.domain.user.UserRepository;
import com.wsw.fitnesssystem.infrastructure.security.model.LoginUser;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.security.core.userdetails.UserDetails;
import org.springframework.security.core.userdetails.UserDetailsService;
import org.springframework.security.core.userdetails.UsernameNotFoundException;
import org.springframework.stereotype.Service;

import java.util.Set;

/**
 * @author loriyuhv
 * @version 1.0 2026/1/14 12:39
 * @since 1.0
 */
@Slf4j
@Service
@RequiredArgsConstructor
public class UserDetailsServiceImpl implements UserDetailsService {
    private final UserRepository userRepository;

    /**
     * Spring Security 认证入口
     *
     * @param username 登录账号（学号 / 工号）
     * @return UserDetails
     * @throws UsernameNotFoundException 用户不存在 / 状态异常
     */
    @Override
    public UserDetails loadUserByUsername(String username) {
        // 1. 查询用户（排除已删除的）
        User user = userRepository.findByUsername(username)
            .orElseThrow(() -> {
                log.warn("用户不存在 username={}", username);
                return new UsernameNotFoundException("用户不存在");
            });

        // 2. 校验用户状态
        if (!user.isEnabled()) {
            log.warn("登录失败：用户已禁用 - userId: {}, username: {}",
                user.getUserId(), username);
            // TODO
            // throw new DisabledException("用户已被禁用，请联系管理员");
        }

        // 3. 查询权限（一次查完）
        Set<String> perms = userRepository.findPermissions(user.getUserId());

        return new LoginUser(user, perms);
    }
}
```

------

### ✅ 第二次 Git commit

```bash
git commit -m "feat(security): implement UserDetails and UserDetailsService"
```

> **feat**：开始有“功能价值”了

------

# 六、第四步：AuthService（真正的登录用例）

## 1️⃣ LoginCommand

```java
public class LoginCommand {
    private String username;
    private String password;
}
```

------

## 2️⃣ AuthService（应用层）

```java
@Service
public class AuthService {

    private final AuthenticationManager authenticationManager;
    private final JwtTokenService jwtTokenService;

    public LoginResult login(LoginCommand command) {

        Authentication authentication =
            new UsernamePasswordAuthenticationToken(
                command.getUsername(),
                command.getPassword()
            );

        Authentication authResult =
            authenticationManager.authenticate(authentication);

        LoginUser loginUser = (LoginUser) authResult.getPrincipal();

        String token = jwtTokenService.createToken(loginUser);

        return new LoginResult(token, loginUser.getUsername());
    }
}
```

👉 这里是 **DDD 的“用例层”**

- 不关心 HTTP
- 不关心 JWT 细节
- 只关心 **“登录这件事是否成功”**

------

### ✅ 第三次 Git commit

```bash
git commit -m "feat(auth): complete login use case and token generation"
```

------

# 七、第五步：Controller（接口层只做转发）

```java
@RestController
@RequestMapping("/auth")
public class AuthController {

    private final AuthService authService;

    @PostMapping("/login")
    public Result<LoginResult> login(@RequestBody LoginCommand command) {
        return Result.success(authService.login(command));
    }
}
```

👉 Controller 的职责只有一句话：

> **HTTP → Command → Service → Result**

------

### ✅ 第四次 Git commit

```bash
git commit -m "feat(api): expose login endpoint"
```

------

# 八、你应该如何“学会用 Git commit”（核心心法）

## ❌ 新手错误

```text
commit: 完成登录功能
```

## ✅ 你的项目应该这样

| 阶段   | commit 类型 | 示例                   |
| ------ | ----------- | ---------------------- |
| 搭结构 | chore       | initialize auth module |
| 加能力 | feat        | implement login        |
| 修 bug | fix         | fix password encoder   |
| 调配置 | config      | adjust security filter |

------

## ⭐ 黄金公式

```
<type>(scope): <动词 + 名词>
```

例子：

```text
feat(auth): add jwt-based login flow
fix(security): handle disabled user login
```

------

# 九、下一步你可以做什么（我给你路线）

如果你愿意继续，我们可以直接 **进阶到生产级**：

1. 🔐 JWT + Redis 登录态管理（踢人 / 过期）
2. 👮‍♂️ 方法级权限 `@PreAuthorize("hasAuthority(...)")`
3. 🧠 数据权限：教师只能看自己班级
4. 🧾 sys_user_login 登录审计全流程落地
5. 🧩 Spring Cloud / Gateway 鉴权迁移方案

------

如果你愿意，**下一条你只要回我一句**👇
👉 **“下一步做 JWT + Security Filter 链路”**

我就直接给你补 **过滤器 + 配置 + 真实生产写法**。